# Admin password for logging into the admin UI
VITE_ADMIN_PASSWORD=

# GitHub token with `gist` scope (optional — used for remote Gist storage)
VITE_GITHUB_TOKEN=

# Gist ID containing `quiplash_state.json` (optional)
VITE_GIST_ID=
